# ESGFP Scoring Tool v4

Zero external chart dependencies — uses only altair (bundled with streamlit).

## Requirements
```
streamlit>=1.28.0
```
That's it. numpy, pandas, altair all come bundled with streamlit.

## Deploy
Push to GitHub → share.streamlit.io → New app → Deploy

## 9-Step Workflow
⓪ Framework Editor → ① Materiality → ② Setup → ③ AHP/FAHP
→ ④ Scoring → ⑤ Summary → ⑥ Scenarios → ⑦ Validation → ⑧ Export
